<%= form_for(spree_current_user.active_relationships.build(followed_id: @designer.id), remote: true)  do |f| %>
  <div><%= hidden_field_tag :followed_id, @designer.id %></div>
  <%= f.submit "Follow", class: "btn btn-primary",style: "width:100px;height:30px;background:#F05F40;color:white; font-weight:bold;" %>
<% end %>