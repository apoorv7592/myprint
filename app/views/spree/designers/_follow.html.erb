<%= form_for(spree_current_user.active_relationships.build(followed_id: @designer.id))  do |f| %>
  <div><%= hidden_field_tag :followed_id, @designer.id %></div>
  <%= f.submit "Follow", class: "btn btn-primary",style: "width:100px;height:30px;background:#87c9c2;color:white;margin-left:200px;" %>
<% end %>