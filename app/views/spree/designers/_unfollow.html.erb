<%= form_for(spree_current_user.active_relationships.where(followed_id: @designer.id).first, html: { method: :delete }, remote: true) do |f| %>
  <%= f.submit "Unfollow", class: "btn",style: "width:100px;height:30px;background:#F05F40;color:white; font-weight:bold;" %>
<% end %>