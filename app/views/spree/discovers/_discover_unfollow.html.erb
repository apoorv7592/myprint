<%= form_for(spree_current_user.discoverfollows.where(discover_id: @discover.id).first, html: { method: :delete }, remote: true) do |f| %>
	<%= hidden_field_tag :discover_id, @discover.id %>
  <%= f.submit "Unfollow", class: "btn",style: "width:100px;height:30px;background:#87c9c2;color:white;" %>
<% end %>	